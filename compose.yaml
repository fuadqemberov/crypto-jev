services:
  crypto-jev:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:8082:8082"
    env_file: .env
    environment:
      DATA_DIR: /app/data
    volumes:
      - analysis:/app/data
    init: true
    stop_grace_period: 45s
volumes:
  analysis:
